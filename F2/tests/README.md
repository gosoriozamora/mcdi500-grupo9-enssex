# Casos controlados de F2

El notebook F2 añade `F2/src` y `F2/tests` a sus rutas de importación e invoca `ejecutar_pruebas(esquema)` desde `validar_preparacion_enssex.py`. Los casos comprueban entradas normales, límites y errores sin depender de gráficos ni de la generación de documentos.
